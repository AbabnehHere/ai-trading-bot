# Production Overrides

bot:
  cycle_interval_seconds: 300

trading:
  min_edge_threshold: 0.07    # Higher threshold in production
  min_confidence: 0.65
  max_open_positions: 8

notifications:
  enabled: true

logging:
  level: "INFO"
